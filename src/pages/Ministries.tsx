import MinistriesSection from "@/components/MinistriesSection";
import PageHeader from "@/components/PageHeader";

const Ministries = () => (
  <>
    <PageHeader eyebrow="Get Involved" title="Our" highlight="Ministries" subtitle="Find your place to grow, serve, and belong." />
    <MinistriesSection />
  </>
);

export default Ministries;
