import GiveSection from "@/components/GiveSection";
import PageHeader from "@/components/PageHeader";

const Give = () => (
  <>
    <PageHeader eyebrow="Support the Ministry" title="Give" highlight="& Partner" subtitle="Your generosity helps us spread the gospel and build God's kingdom." />
    <GiveSection />
  </>
);

export default Give;
